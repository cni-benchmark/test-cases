apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: core
spec:
  path: &path "${KS_PATH}/cluster-api/core"
  postBuild:
    substitute:
      KS_PATH: *path
      NAMESPACE: system
  dependsOn:
    - name: system
