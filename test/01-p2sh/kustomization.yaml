apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: 01-p2sh
resources:
  - ../lib/namespace-client-server
patches:
  - target:
      kind: Service
      name: server
    patch: |-
      - op: replace
        path: /spec/clusterIP
        value: None
