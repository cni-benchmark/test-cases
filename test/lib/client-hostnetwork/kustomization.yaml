apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
patches:
  - target:
      kind: Pod
      name: client
    patch: |-
      - op: replace
        path: /spec/hostNetwork
        value: true
      - op: replace
        path: /spec/dnsPolicy
        value: ClusterFirstWithHostNet
