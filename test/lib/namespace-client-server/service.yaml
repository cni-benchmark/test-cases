apiVersion: v1
kind: Service
metadata:
  name: server
spec:
  selector:
    iperf3: server
  ports:
    - name: tcp
      port: 5201
      protocol: TCP
      targetPort: tcp
    - name: udp
      port: 5201
      protocol: UDP
      targetPort: udp
