apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: default
  namespace: default
spec:
  path: &path "${KS_PATH}/default"
  postBuild:
    substitute:
      KS_PATH: *path
      NAMESPACE: default
